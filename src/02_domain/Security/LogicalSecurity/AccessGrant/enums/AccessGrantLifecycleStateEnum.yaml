id: https://example.org/edm/domain/Security/enums/AccessGrantLifecycleStateEnum
name: domain_security_access_grant_lifecycle_state_enum
title: Domain (Security) — AccessGrantLifecycleStateEnum
description: Lifecycle states for an AccessGrant.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  AccessGrantLifecycleStateEnum:
    description: Lifecycle states for an AccessGrant.
    permissible_values:
      REQUESTED:
      PENDING_APPROVAL:
      APPROVED:
      PROVISIONED:
      ACTIVE:
      SUSPENDED:
      REVOKED:
      EXPIRED:
