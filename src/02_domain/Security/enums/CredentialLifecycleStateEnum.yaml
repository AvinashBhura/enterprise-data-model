id: https://example.org/edm/domain/Security/enums/CredentialLifecycleStateEnum
name: domain_security_credential_lifecycle_state_enum
title: Domain (Security) — CredentialLifecycleStateEnum
description: Lifecycle states for an AccessCredential.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  CredentialLifecycleStateEnum:
    description: Lifecycle states for an AccessCredential.
    permissible_values:
      ISSUED:
      ACTIVE:
      LOST:
      RETURNED:
      EXPIRED:
      REVOKED:
      DESTROYED:
