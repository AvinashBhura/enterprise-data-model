id: https://example.org/edm/domain/Finance/enums/BudgetLifecycleStateEnum
name: domain_finance_budget_lifecycle_state_enum
title: Domain (Finance) — BudgetLifecycleStateEnum
description: Lifecycle states for a Budget.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  BudgetLifecycleStateEnum:
    description: Lifecycle states for a Budget.
    permissible_values:
      DRAFT:
        description: Being drafted.
      PROPOSED:
        description: Proposed; awaiting approval.
      APPROVED:
        description: Approved and active.
      ACTIVE:
        description: In effect.
      LOCKED:
        description: Locked; no further changes permitted.
      CLOSED:
        description: Period ended; budget closed.
      ARCHIVED:
        description: Archived.
