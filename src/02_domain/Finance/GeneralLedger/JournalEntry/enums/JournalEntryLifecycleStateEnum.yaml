id: https://example.org/edm/domain/Finance/enums/JournalEntryLifecycleStateEnum
name: domain_finance_journal_entry_lifecycle_state_enum
title: Domain (Finance) — JournalEntryLifecycleStateEnum
description: Lifecycle states for a JournalEntry.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  JournalEntryLifecycleStateEnum:
    description: Lifecycle states for a JournalEntry posting.
    permissible_values:
      DRAFT:
        description: Being prepared.
      PENDING_APPROVAL:
        description: Awaiting approval.
      POSTED:
        description: Posted to the GL.
      REVERSED:
        description: Reversed via a reversing entry.
      VOIDED:
        description: Voided before posting.
