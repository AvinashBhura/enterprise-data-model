id: https://example.org/edm/domain/HumanResources/enums/PositionLifecycleStateEnum
name: domain_hr_position_lifecycle_state_enum
title: Domain (HR) — PositionLifecycleStateEnum
description: Lifecycle states for a Position.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  PositionLifecycleStateEnum:
    description: Lifecycle states for a Position.
    permissible_values:
      DRAFT:
      APPROVED:
      OPEN:
      FILLED:
      ON_HOLD:
      ELIMINATED:
