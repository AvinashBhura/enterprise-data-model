# Application — _shared

Cross-application structures used by multiple vendor projections —
common sync metadata, status enums, source-system reference shapes.

## Entities
- `SyncMetadata` (value type) — common sync tracking attributes.
- `SourceSystemReference` (value type) — universal source-system binding.

## Enum
- `SyncStatusEnum` — generic sync states.
