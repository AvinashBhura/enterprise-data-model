id: https://example.org/edm/application/ServiceNow/enums/ServiceNowSyncStatusEnum
name: application_servicenow_sync_status_enum
title: Application (ServiceNow) — ServiceNowSyncStatusEnum
description: ServiceNow-specific sync states.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  ServiceNowSyncStatusEnum:
    description: ServiceNow-specific sync states.
    permissible_values:
      PENDING:
      IN_PROGRESS:
      SYNCED:
      OUT_OF_SYNC:
      SNOW_TABLE_LOCKED:
      RETIRED:
