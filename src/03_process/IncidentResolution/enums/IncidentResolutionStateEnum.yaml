id: https://example.org/edm/process/IncidentResolution/enums/IncidentResolutionStateEnum
name: process_incident_resolution_state_enum
title: Process (IncidentResolution) — IncidentResolutionStateEnum
description: Workflow states for an IncidentResolutionProcessInstance.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  IncidentResolutionStateEnum:
    description: Workflow states for incident resolution.
    permissible_values:
      DETECTED:
      TRIAGED:
      ASSIGNED:
      INVESTIGATING:
      WORKAROUND_APPLIED:
      MITIGATED:
      RESOLVED:
      VERIFIED:
      CLOSED:
      REOPENED:
      ESCALATED:
      CANCELLED:
