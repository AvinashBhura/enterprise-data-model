id: https://example.org/edm/process/_core/enums/ProcessEventTypeEnum
name: process_core_event_type_enum
title: Process (_core) — ProcessEventTypeEnum
description: Types of events recorded on a ProcessInstance.

license: Apache-2.0
version: 0.1.0

prefixes:
  linkml: https://w3id.org/linkml/
  edm: https://example.org/edm/

default_prefix: edm
default_range: string

imports:
  - linkml:types

enums:
  ProcessEventTypeEnum:
    description: Types of events recorded for event sourcing.
    permissible_values:
      INSTANCE_STARTED:
      STEP_STARTED:
      STEP_COMPLETED:
      STEP_SKIPPED:
      STEP_FAILED:
      INSTANCE_PAUSED:
      INSTANCE_RESUMED:
      INSTANCE_COMPLETED:
      INSTANCE_CANCELLED:
      INSTANCE_MIGRATED:
      EXTERNAL_EVENT:
        description: A relevant external event observed.
      COMMENT_ADDED:
      ASSIGNMENT_CHANGED:
